services:
  api:
    build:
      context: .
      dockerfile: ./Rockfall-Prediction-Model/api/Dockerfile
    ports:
      - "8000:8000"

  dashboard:
    build: ./terrain-alert-ai
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://api:8000
    depends_on:
      - api

